## Usage

To run the updater:

```sh
$ gen {%= alias %}
```

To run a specific task, just run `$ gen {%= alias %}:` followed by the name of the task to run:

```sh
$ gen {%= alias %}:del
$ gen {%= alias %}:copy
```